#! /bin/bash

RPM1=2000
RPM2=3500

RPM=$(sensors | grep -i fan | awk '{print $2}' )
RPM=${RPM%}

if [ "$RPM" -ge "$RPM2" ] ; then
    echo -e "%{F#f00} $RPM RPM"
elif [ "$RPM" -ge "$RPM1" ] ; then
    echo -e "%{F#ffa500} $RPM RPM"
else
    echo -e "%{F#00ff00} $RPM RPM"

fi
